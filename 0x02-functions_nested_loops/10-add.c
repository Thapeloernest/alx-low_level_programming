#include "main.h"

/**
 * add - adds the integers and returns the results
 *
 * @x: number being added
 *
 * @y: number being added
 *
 * Return: Always 0.
 */
int add(int x, int y)
{
	return (x + y);
}
